# 사용자 확인 대기 목록 (누적)

에이전트가 구현/빌드/배포까지는 끝냈지만 **실제로 눌러봐야 알 수 있는 것들**을 모아둔
체크리스트. 한 번에 몰아서 확인할 수 있게 배치로 쌓는다.

- 확인 끝난 항목은 `[x]`로 바꾸고, 문제가 있으면 그 자리에 증상을 적을 것
- 배치 전체가 끝나면 이 파일에서 지우고 해당 plan 문서 쪽에 결과를 반영
- 관련 계획/원인 분석은 `.claude/backlog/qa-batch-2026-09-03.md` 참고

---

## 배치 1 — 2026-09-03 (빌드/배포 완료, 번들 `index-SFvDIV7f.js`)

### 사이드바

- [x] **모바일에서 사이드바를 닫았을 때 왼쪽 가장자리에 그림자 띠가 안 남는가**
      (열려 있을 때는 그림자가 여전히 보여야 함. 열고 닫을 때 그림자가 슬라이드와
      같이 0.2s로 페이드되는지도 같이 봐주세요 — 어색하면 `transition`에서 빼면 됨)
- [ ] **사이드바 하단 잠금 표시가 "곧 만료"에 고정되지 않는가**
      잠금 해제 후 10분 이상 두고, 중간에 다른 탭에서 뭔가 조작한 뒤 확인.
      남은 시간이 다시 늘어나야 정상(세션이 sliding이라 조작할 때마다 갱신됨).
      30초 주기로 갱신되므로 즉시 반영은 아님

### 터미널

- [x] **창 포커스 시 크기 재조정** — 폰/태블릿에서 같은 세션을 열어 크기를 바꿔놓고,
      데탑/랩탑 창을 다시 포커스했을 때 터미널이 이쪽 크기로 맞춰지는가
      (포커스 순간 1회만 발사. 홈 탭에서는 동작 안 함)
- [x] **"code로 열기" 버튼** — 터미널 상단 액션에 새로 생김
  - [x] 그냥 클릭 → 현재 cwd가 code-server에서 열리는가
  - [x] **가운데 클릭 / Ctrl+클릭 → 새 탭**에서 열리는가
  - [x] `cd`로 디렉터리를 옮긴 뒤 최대 3초 안에 링크가 새 경로를 가리키는가
        (3초 폴링 값을 씀 — 기존 "파일 브라우저에서 열기"는 클릭 시점에 조회해서
        지연이 없는데, 이건 앵커라 렌더 시점 값이 필요했음)
  - [x] webmanager를 iframe 안에서 쓰는 경우 iframe 안이 아니라 최상위에서 열리는가

### 파일 매니저

- [x] **"code로 열기" 액션** — "터미널에서 열기" 옆에 새로 생김
  - [x] 현재 보고 있는 폴더가 열리는가 / 가운데 클릭 새 탭
  - [ ] 최상위 빈 폴더 같은 경우 비활성 상태로 보이고 눌리지 않는가
      > 최상위 빈 폴더가 무엇을 의미하는지 모르겠음

### 잠금(비밀번호 게이트)

- [ ] **프로젝트 상세 다이얼로그를 열 때 잠금 해제가 딱 한 번만 뜨는가**
      (이전엔 모달 + 인라인 카드 두 개가 떴음)
  - [x] Projects 탭에서 행 클릭으로 열 때 — 2026-09-04 브라우저로 확인.
        프롬프트 **정확히 1개**, 다이얼로그가 열리기 *전에* 뜸(뒤에 상세 시트가
        열려 있지 않은 것까지 확인)
  - [ ] 터미널 탭의 "프로젝트 정보" 버튼으로 열 때
  - [x] **잠금 해제를 취소하면 다이얼로그가 아예 안 열리는가** — 확인.
        취소 시 프롬프트만 닫히고 상세 시트는 열리지 않음
  - [ ] 이미 잠금 해제된 상태면 추가 프롬프트 없이 바로 열리는가
  - [ ] 게이트가 아예 설정 안 된 환경에서 프롬프트가 안 뜨는가

### 확장 추천

- [x] **Extensions 탭에 VSCord가 "편의 기능" 카테고리로 뜨는가**
- [x] 클릭해서 **실제로 설치가 되는가** (Open VSX id `LeonardSSH.vscord`)
- [x] 새 카테고리 `편의 기능`이 UI에서 정상적으로 그룹으로 렌더되는가
      (기존 카테고리 3개 어디에도 안 맞아서 새로 만든 것)

---

## 배치 2 — 2026-09-03 (빌드/배포 완료, 번들 `index-B1oCk1zX.js`)

### 프로세스 테이블

- [x] **헤더와 본문 열이 정확히 맞는가** — 2026-09-04 브라우저로 실측 확인.
      처음 [x] 표시하셨을 때는 사실 **여전히 어긋나 있었습니다**(헤더가 왼쪽으로
      밀려 있었음). 실측으로 원인 두 개를 찾아 고쳤고, 이제 9개 열 전부
      좌표 오차 **0.00px**이며 정렬 기준을 7개 열로 바꿔가며 재측정해도 전부 0.00px.
- [ ] **행들이 y축으로 어긋나지 않는가.** 원인은 `.pf-col-name`/`.pf-col-user`에
      `white-space: nowrap`이 없어서 `text-overflow: ellipsis`가 무력화돼 있던 것 —
      **이름이나 사용자명이 아주 긴 프로세스가 있는 상태에서** 봐주세요.
      이제 두 줄로 접히지 않고 `…`로 잘려야 함
      > td.pf-col-status 에서 문제가 발생. 실측 상 그 부분은 내부 컨텐츠가 `63*21.510` 으로 다른 요소인 pf-col-user 가 `63*19.120` 인 것에 비해서 height 가 큼을 확인. 필요 시 claude-in-chrome 을 subagent 를 사용해 실측해도 좋음
      >
      > **(에이전트 실측 2026-09-04)** 행 높이는 이제 전부 44.55px로 동일합니다
      > — 행 간 드리프트는 없습니다. 지적하신 status 셀이 다른 셀보다 높은 건
      > 맞지만(badge 22.25 vs 텍스트 19.13), 행이 `align-items: center`라
      > 세로 중앙 정렬되므로 시각적 어긋남으로는 이어지지 않습니다.
      > 실제로 눈에 보이던 문제는 **열(가로) 어긋남**이었고, 그건 위 항목에서
      > 고쳤습니다. 남은 것은 헤더 행 40.5px vs 본문 행 44.55px인데,
      > 이건 본문에만 있는 종료 버튼 높이 때문이고 정렬에는 영향이 없습니다.
- [x] 헤더 행이 본문 행보다 눈에 띄게 낮아 보이지 않는가 (`min-height: 2.7rem` 추가)
- [x] 프로세스 트리(들여쓰기) 표시가 그대로인가
- [x] 정렬(컬럼 클릭)과, 정렬 시 스크롤이 튀지 않는 기존 동작이 유지되는가
      (`order` 기반 재정렬은 일부러 그대로 뒀음)
- [x] 포트 탭 테이블은 손대지 않았으니 그대로여야 함

### 터미널 — 자동 재연결 (신규, 기본 켜짐)

- [x] **모바일에서 앱을 백그라운드로 보냈다가 다시 오면 알아서 재연결되는가**
      (포그라운드 복귀 시엔 백오프를 기다리지 않고 즉시 재시도)
- [ ] 재연결 대기 중 오버레이에 "재연결 중... (N번째 시도)" 와
      "X초 후 다시 시도" 카운트다운이 보이는가
- [ ] "지금 재연결" 버튼이 즉시 재시도하고 백오프를 리셋하는가
- [ ] 설정 패널의 "자동 재연결" 체크박스로 끄면 예전처럼 가만히 있는가
      (이 토글은 탭을 다시 열 필요 없이 **즉시** 적용돼야 함 — 모바일 입력
      워크어라운드 토글과 다른 점)
- [ ] **터미널에서 Ctrl+D로 셸을 종료했을 때** 그 탭이 재연결로 되살아나지 않고
      홈 탭으로 떨어지는가 ← 이게 제일 중요한 회귀 포인트
- [ ] 서버를 잠깐 내렸다 올렸을 때 무한 폭주 없이 1s→2s→4s…30s로 늘어나며
      재시도하는가
- [ ] 재연결 대기 중 다른 탭으로 전환하면 대기 중이던 재시도가 취소되는가

### 터미널 — 세션명 편집

- [x] **탭이 2개 이상일 때 두 번째/세 번째 세션 탭의 연필 버튼으로 이름 편집이 되는가**
      (포커스가 들어가고, 타이핑이 먹고, 다른 곳 클릭 시 닫히는가)
- [x] 편집 중이 아닐 때 **탭 드래그 재정렬이 여전히 되는가** ← 이번 변경의 주 회귀 위험
- [x] blur로 이름이 저장되고 Escape로 취소되는가
- [x] 홈 탭 이름 변경도 그대로 되는가
      > 다른 이슈이지만, 홈 아이콘이 약간 더 위쪽으로 올라가 있음을 확인. 그리고 홈탭은 이름을 변경할 이유가 없었는데, 왜 있는지? 또 텍스트와 아이콘 사이 공간이 없어서 붙어있다는 점을 봄

> **확인차 여쭤볼 것**: "첫 번째 탭은 됐다"고 하신 그 첫 번째가 **홈 탭**이었나요?
 => 아니요. 진짜 첫 세션이요. 첫 오더에 있는 세션.
> 홈 탭은 코드상 `draggable`이 아니고 세션 탭은 전부 `draggable`인데,
> `draggable=true` 조상이 중첩 `<input>`의 클릭/포커스를 삼키는 건 알려진 동작이라
> 증상(포커스 안 감 / 클릭도 안 됨 / blur가 안 나서 안 닫힘)이 정확히 맞습니다.
> 홈 탭이 맞다면 이게 근본 원인이고, 아니라면 이번 수정은 방어적 하드닝에 그칩니다.
> (원래 유력 가설이던 "오버레이가 탭바를 덮는다"는 코드 확인 결과 **아님** —
> 오버레이는 `.terminal-surface` 안에 갇혀 있어 탭바에 닿지 못합니다.)

---

## 배치 3 — 2026-09-04 (빌드/배포 완료, 번들 `index-Cy3Nna0A.js`)

### code-server 자동 포트 포워딩

- [ ] **PORTS 탭이 dev 서버를 자동으로 잡아가지 않는가**
      (컨테이너에 `settings.json`이 없어서 방금 실제로 시드됐음 —
      로그에 `code-settings: seeded ...` 확인함. 파일 열어보면 주석과 함께
      `"remote.autoForwardPorts": false`가 들어있음)
- [ ] code-server 설정 UI에서 이 값을 직접 켜면 다시 자동 포워딩이 되는가
      (사용자 편집이 존중되는지 — 재시작해도 다시 false로 덮이면 안 됨)
- [ ] 재시작했을 때 로그에 `code-settings:` 경고가 안 뜨는가
      (키가 이미 있으므로 조용해야 정상)

### Claude Code 탭

- [ ] 탭이 정상적으로 뜨고 로그인 상태가 제대로 보이는가
- [ ] **노트북을 재웠다 깨운 뒤 "새로고침"이 동작하는가** ← 원래 신고하신 증상.
      이전엔 멈춘 fetch 때문에 영구 no-op이 됐음
- [ ] 상태 조회가 실패했을 때 예전 카드가 그대로 남지 않고
      "확인 실패" + "다시 확인" 버튼이 뜨는가
      (백엔드가 이제 실패 사유를 `authError`로 내려줌)

### API 타임아웃 (전역 15초) — 회귀 위험 구간

`api/client.ts`에 15초 기본 타임아웃이 생겼습니다. 오래 걸리는 호출은 예외 처리했지만
실사용으로 확인이 필요합니다.

- [ ] **확장 설치/삭제**가 끝까지 되는가 (65초로 예외 처리함)
- [ ] **폰트 설치**가 끝까지 되는가 (65초로 예외 처리함)
- [ ] mise 도구 설치, git clone — 이들은 즉시 반환하는 작업 시작 POST라
      기본값 그대로 뒀습니다. 실제로 15초 안에 응답이 오는지 확인
- [ ] 파일 업로드/다운로드가 그대로 되는가 (`client.ts`를 안 거치므로 영향 없어야 함)
- [ ] 느린 응답에서 "요청 시간이 초과되었습니다"가 뜨면 알려주세요 —
      해당 호출만 예외 처리하면 됩니다

> **미해결로 남긴 것**: `POST /system/disk-breakdown/scan`(사용자가 누르는 `du` 스캔)은
> 백엔드에 타임아웃이 아예 없어서 15초를 넘길 수 있습니다. 다른 에이전트가
> `Processes/`를 잡고 있어서 이번엔 안 건드렸습니다 — 디스크 사용량 스캔이
> 시간초과로 실패하면 이것 때문입니다.

---

## 배치 4 — 2026-09-04 (빌드/배포 완료, 번들 `index-DLp94f7R.js`)

### 하단 컨트롤 바 숨기기 + 줌 이동

- [ ] **데스크탑에서 하단 바가 기본으로 숨겨져 있는가**
      (기본값은 `matchMedia('(pointer: fine)')` 기준. 하이브리드/터치 랩탑에서
      이 값이 기대대로 나오는지가 미검증 지점이니 특히 봐주세요)
- [ ] 모바일/태블릿에서는 여전히 기본으로 보이는가
- [ ] 설정 패널 "특수키 (컨트롤 바)" 섹션의 체크박스로 켜고 끌 때
      **탭을 다시 열지 않아도 즉시** 반영되는가
- [ ] **바가 숨겨졌을 때 탭 줄 맨 오른쪽에 `−` `+`가 나타나는가**
  - [ ] 세로로 가운데 정렬돼 있는가 ← 명시 요구사항
  - [ ] **탭이 2~3줄로 줄바꿈됐을 때도** 오른쪽 끝에 붙어 있고 세로 중앙인가
        (좁은 화면에서 확인 필요 — 미검증 지점)
  - [ ] 줌이 실제로 동작하는가 (하단 바의 줌과 같은 동작)
- [ ] 바가 보일 때는 탭 줄에 줌이 **안** 보이는가 (둘이 동시에 뜨면 안 됨)
- [ ] **탭 드래그 재정렬이 여전히 되는가** (탭들이 DOM에서 한 단계 더 깊어졌음)
- [ ] 탭 이름 편집, 홈 탭, 탭 줄바꿈 동작이 그대로인가

### xterm 하단 잘림 — 원인 확정, 수정됨 (번들 `index-_be9OZs9.js` / CSS `index-Cev-21gl.css`)

프로덕션 실측으로 원인이 산수까지 맞아떨어졌습니다. `index.css:192-196`이 전역
`box-sizing: border-box`를 걸어두는데, Chrome은 그 경우 `getComputedStyle().height`로
**border box**를 돌려줍니다. FitAddon은 그 값을 컨테이너의 *콘텐츠* 높이로 믿고
행 수를 계산하므로, `.terminal-container`에 있던 패딩만큼 행이 과하게 잡혔습니다:

```
getComputedStyle(container).height  384.233   <- FitAddon이 읽는 값
container padding                   6px / 0px
.xterm height                       378.233   <- 384.233 - 6, 실제
.xterm padding                      0 / 0     <- 뺄 게 없음
rows / cellH                        24 / 16
.xterm-screen height                384       <- 24 * 16
초과                                5.767px   = 0.36행, overflow:hidden에 잘림
```

**잘린 양 = 컨테이너의 세로 패딩.** 기기별 편차도 이걸로 설명됩니다(패딩은 rem,
셀 높이는 폰트/DPR을 따라가므로 비율이 기기마다 다름).

수정: 패딩을 `.terminal-container`에서 **`.xterm` 자신으로 옮겼습니다.** FitAddon은
`.xterm`의 패딩을 `elementPaddingVer`로 명시적으로 빼주므로, 이제 여백이 실제로
행 수에 반영됩니다. (그 전에 제가 넣었던 컨테이너 아래 패딩은 이 문제를 키우는
방향이었어서 함께 되돌렸습니다.)

- [x] **마지막 줄이 더 이상 잘리지 않는가** — 2026-09-04 사용자 확인 완료.
      원래 잘리던 PC에서 화면 크기를 그대로 두고 재현 시도했으나 잘리지 않음.
      산수(초과분 = 컨테이너 세로 패딩)와 실측이 일치 → **원인 확정**
- [ ] 터미널 상하좌우 여백이 어색하지 않은가 (이제 아래에도 여백이 생겼습니다)
- [ ] 확인용 스니펫 — `overflow`가 0 이하로 나와야 정상입니다:

```js
const q = s => document.querySelector(s)
const x = q('.terminal-container .xterm'), scr = q('.xterm-screen')
const xs = getComputedStyle(x)
console.log({
  xtermContent: parseFloat(xs.height) - parseFloat(xs.paddingTop) - parseFloat(xs.paddingBottom),
  screen: scr.getBoundingClientRect().height,
  overflow: scr.getBoundingClientRect().height
    - (parseFloat(xs.height) - parseFloat(xs.paddingTop) - parseFloat(xs.paddingBottom)),
})
```


---

## 브라우저 실측 메모 (2026-09-04)

에이전트가 claude-in-chrome으로 `http://yaeji-laptop/manager`(테스트 스택)에서 직접
확인한 항목은 위에 `[x]` + 근거를 적어뒀습니다.

**에이전트가 확인할 수 없는 것**: Terminal / Files / Sessions 탭과 프로젝트 상세 안쪽은
비밀번호 게이트 뒤에 있는데, **에이전트는 비밀번호를 입력하지 않습니다**(테스트용이라도
자격증명 입력은 하지 않는 쪽). 브라우저에서 직접 한 번 잠금 해제해두시면 이어서
실측할 수 있습니다 — 세션 TTL이 10분 sliding이라 그 사이 조작이 있으면 유지됩니다.

해제 후 에이전트가 이어서 볼 수 있는 것:
- 홈 탭 아이콘이 라벨보다 위로 뜨는 문제, 아이콘-텍스트 간격
- 하단 컨트롤 바 기본 숨김 여부(`(pointer: fine)` 판정), 토글 즉시 반영
- 줌 그룹이 탭 줄 오른쪽 끝 + 세로 중앙인지, 탭이 여러 줄로 접혔을 때도 유지되는지
- 터미널 마지막 줄 잘림 재확인(현재 배포본 기준)
- 터미널 탭의 "프로젝트 정보" 버튼 경로에서도 잠금이 1회만 뜨는지
