사람이 계획중인 마일스톤과 작업입니다. 에이전트가 보고 추가해도 되나, 기본적으로 에이전트가 행할(생각할) 필요는 없는 작업 모임입니다. 필요 시 append 는 수행해도 좋습니다.

- 에이전트가 쓰기 쉬운 환경으로 만들려면 어떻게 해야하는가? : mise 나 여러 도구들에 대해서, 특히 code-docker 환경에 대해서 어느정도 문서화 하고 스킬을 제공하거나 해야할듯. 또 나는 api 상 에이전트가 쓰기 좋은것도 좀 있는듯 한데 mcp 로 제공해도 될것 같아보임. 명령줄 쓰면 토큰 많이 갈아먹으니까 api 로 간단하게 요청처리 되도록 mcp 를 제공해도 될듯. 생각 더 해보고 에이전트에 요구사항을 넣을 수 있을 만큼 이해가 생기면 넘기는 것으로.
- 키보드 모드키 위치는 모바일에서 키보드가 뜨는 바로 위에 뜨게 아래 있는게 좋아보이긴 함. 그리고 dvh 따르나 봐야함, 화면에 키보드 올라왔는데 높이 그대로면 문제가 생김. 사용해보고 문제 되는지 보고 에이전트에게 알려야함
- webmanager 없이 심플한 이전 느낌의 code-docker 를 원하는 유저도 분명 있으므로 나중에 적당히 dockerfile 을 나눠볼 계획. 혹은 dockerfile 에 arg 로써 opt out 할 수 있게 만들 예정. 이를 위해 분리해야하는 script 나 여러 사항은 복잡하므로 에이전트 없이 직접 결정할 예정.
- paste.qwreey.moe/pastebin 같은거에 연동하여 바로 올려지는 그런거 만들어보자. code 자체 ui 상 된다면 좋고
- 문서화는 차라리 블로그 서비스 만들고 있는것에 기대는게 좋을수도 있겠음. webui 에 넣으면 너무 연관성이 심해지기도 하고, 난잡해질듯. 그냥 ? (info) 버튼 하나 만들고 블로그를 열도록 하고, url 을 유저 지정하게 두는게 현명해보임. 아직 에이전트는 이 프로젝트를 자세히 모르니 이곳 상황이 괜찮아지면 그때 진행을 하면 됨.
- (2026-08-03, 에이전트가 추가) webmanager 쪽에 사람이 직접 확인해야 할 항목들 — 에이전트는 실제 설치/외부 상태 변화를 확인할 방법이 없어서 코드만으로는 검증이 안 됨:
  - mise 탭에서 실제로 도구 install/use/uninstall이 되는지
  - Code Extensions 탭에서 실제로 익스텐션 install이 되는지
  - 위 둘 다 설치된 목록/추천 목록이 실제로 제대로 나오는지(카테고리 분류, 접기/펼치기, 표시 토글 포함)
  - Projects 탭도 한번 훑어봐야 함(스캔 결과/재생성 가능 폴더 탐지/mise 도구 표시가 실제 프로젝트 디렉토리 기준으로 맞게 나오는지)
- (2026-08-03, 에이전트가 추가) webmanager를 PWA로 설치해서 쓰기 — Ctrl+W가
  브라우저 탭에서는 OS/브라우저 차원에서 예약된 조합키라 페이지 JS로 아예
  keydown이 안 와서 터미널 탭에서 못 막지만, 설치된 PWA 창(탭이 아님)에서는
  브라우저가 그 조합키를 가로채지 않아서 정상적으로 막고 셸로 전달 가능
  (사용자가 code-server/Termix에서 직접 확인). `expose.md`(레포 루트)의
  code-server+webmanager 라우트 통합 계획이 실현되면 통합 PWA 하나 설치로
  자연히 해결됨.
- 버저닝이 필요함, 테그를 사용하던가 해야함. code-docker 는 변화가 이제 에이전트로 가속되어 빨라짐. 따라서 중간단계 커밋을 다른 브런치로 나누던가 해야할수도 있음. 이건 생각을 해봐야할 문제. 근데 나는 tag 를 두고 pull 을 테그 전체 되도록 한다음 최신 tag 로 이동하는걸 선호함. 버전 선택의 자유가 있기 때문. master 에는 릴리즈만 넣는다는 이정도까지 못함. readme 변경과 이에 대한걸 논의해봐야함.
- (2026-08-04, 사용자 제보, 에이전트가 추가) 컨테이너를 빠르게 시작하고 바로
  취소(`docker compose up` 직후 Ctrl+C 등)하면 stopping 시그널이 제대로
  처리되지 않음 — `entrypoint.sh`가 SIGTERM을 supervisord로 제대로 트랩/전달
  안 하거나, supervisord 자체가 아직 기동 중일 때 stop 신호를 받아서 무시하는
  등의 원인으로 추정. 중요도는 낮음(사용자 본인 평가) — 급하게 다룰 필요는
  없고, 나중에 `entrypoint.sh`의 시그널 트랩 처리와 supervisord 기동 타이밍을
  같이 살펴봐야 함.
- (2026-08-19, 사용자 제보, 에이전트가 추가) webmanager 터미널의 모바일 입력
  워크어라운드(실험적, 터미널 설정 패널에서 끄고 켤 수 있음, localStorage) —
  가상 키보드 예측입력 때문에 글자가 스페이스 누르기 전까진 버퍼링되던 문제와
  비밀번호 저장 자동완성 팝업은 실기기 확인상 해결됨. 다만 연속으로 빠르게
  입력하면 여전히 입력이 씹히는 속도 문제가 남아있음 — 매 키 입력마다
  `requestAnimationFrame`으로 숨겨진 `<input type="password">`의 값을
  리셋하는 방식이 원인으로 추정되지만 에이전트가 직접 프로파일링은 못 해봐서
  확실친 않음. 나중에 실기기(안드로이드 Chrome)로 붙어서 디버깅 필요 — 관련
  코드는 `webmanager/frontend/src/components/Terminal/Terminal.tsx`의 xterm
  생성 이펙트 안 `mobileInput` 관련 블록.
